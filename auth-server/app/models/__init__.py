# app/models/__init__.py

"""
This module defines the models for interacting with the database.
Currently, it is a bit coupled with the mongo, but can be easily changed to other databases.
These models are by services to call repositories and repositories to execute queries.
"""