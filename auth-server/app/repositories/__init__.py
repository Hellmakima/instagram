"""
Folder: app/api

Contains all the api endpoints that are called by the client
Take in Models as input (if necessary)
returns Models (Maybe skip only if return type is too insignificant to define e.g., only id string returned)
raises Exceptions if something goes wrong
errors are handled by the service layer
"""
